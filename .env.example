# Public Supabase client values (safe for frontend builds).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Server-side / seed scripts only — never expose in frontend or Cloudflare Worker env.
# SUPABASE_SERVICE_ROLE_KEY=
